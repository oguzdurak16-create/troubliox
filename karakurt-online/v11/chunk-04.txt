='dungeon'?'dungeon':currentMap==='pvp'?'pvp':'');message(`Bölge: ${MAPS[currentMap].name}`)}
function message(t){ui.msg.textContent=t;const p=document.createElement('p');p.innerHTML='<b>[System]</b> '+t;ui.chat.appendChild(p);while(ui.chat.children.length>5)ui.chat.removeChild(ui.chat.firstChild)}
function setTarget(m){target=m;if(!m||!m.alive){target=null;ui.target.classList.add('hidden');return}ui.target.classList.remove('hidden');ui.tname.textContent=m.name;ui.tlevel.textContent=`Lv ${m.level}${m.boss?' · BOSS':''}`;updateTarget()}
function updateTarget(){if(target&&target.alive)ui.thp.style.width=(target.hp/target.maxHp*100)+'%'}
function nearest(max=32){let best=null,bd=max;for(const m of mobs){if(!m.alive||m.map!==currentMap)continue;const d=player.position.distanceTo(m.mesh.position);if(d<bd){bd=d;best=m}}return best}
function targetNearest(){const m=nearest();if(m){setTarget(m);message(`Hedef: ${m.name}`)}else message('Yakında hedef yok.')}
function toggleAuto(){if(state.dead)return;if(!target||!target.alive)setTarget(nearest());state.auto=!state.auto;if(!target)state.auto=false;message(`Basic auto attack ${state.auto?'AÇIK':'KAPALI'}.`);renderHud()}
function toggleAfk(){if(currentMap==='pvp'){message('AFK farm PvP bölgesinde kapalı.');return}state.afk=!state.afk;if(state.afk){state.auto=true;if(!target)setTarget(nearest())}message(`AFK slot modu ${state.afk?'AÇIK':'KAPALI'}.`);renderHud()}
function damagePlayer(n,source=null){const now=performance.now();if(state.dead||isSafeZone()||now<state.invulnUntil)return;if(now<state.guardUntil)n*=.35;state.hp=Math.max(0,state.hp-Math.round(n));state.screenShake=Math.min(.65,state.screenShake+.22);state.hitStunUntil=now+220;setCombatState('hit',state.hitStunUntil);if(source){const kb=player.position.clone().sub(source).setY(0);if(kb.lengthSq()>.001){kb.normalize();player.position.addScaledVector(kb,.28)}}if(state.hp<=0)die();renderHud()}
function die(){state.dead=true;state.auto=false;state.afk=false;pendingAttack=null;setCombatState('dead');keys.w=keys.a=keys.s=keys.d=false;ui.death.classList.add('open');message('Öldün. Karahisar yeniden doğuş noktasına dön.')}
function respawn(){currentMap='bozkir';state.dead=false;state.hp=state.maxHp;state.mp=state.maxMp;state.stamina=state.maxStamina;state.invulnUntil=performance.now()+1200;setCombatState('idle');ui.death.classList.remove('open');syncVisibility();renderHud()}
function gainKill(m){state.xp+=m.exp;state.coins+=m.coins;if(m.kind==='enemy')state.np+=10;if(currentMap==='bozkir'&&m.kind==='wolf')state.kills++;const drops=['Kurt Dişi','Eski Bronz Parçası','Minor Potion','Keten Kumaş'];if(Math.random()<.55)state.loot.push(drops[Math.floor(Math.random()*drops.length)]);while(state.xp>=state.nextXp){state.xp-=state.nextXp;state.level++;state.nextXp=Math.round(state.nextXp*1.32);state.maxHp+=55;state.maxMp+=16;state.hp=state.maxHp;state.mp=state.maxMp;message(`LEVEL UP! Seviye ${state.level}.`)}if(state.kills>=5)$('#questText').textContent='5/5 Kurt öldürüldü. Karahisar Muhafızı ile konuş.';renderHud()}
function killMob(m){m.alive=false;m.mesh.visible=false;m.respawnAt=performance.now()+(m.boss?22000:8000);gainKill(m);if(target===m)setTarget(null);state.auto=false;if(state.afk){setTimeout(()=>{const n=nearest();if(n){setTarget(n);state.auto=true}},450)}message(`${m.name} öldürüldü. +${m.exp} EXP +${m.coins} coin`)}
function hit(m,dmg,tag='R',canCrit=true){if(!m||!m.alive)return;const now=performance.now(),crit=canCrit&&Math.random()<.12;if(crit)dmg*=1.65;dmg=Math.round(dmg);m.hp=Math.max(0,m.hp-dmg);m.hitStunUntil=now+(m.boss?90:150);if(!m.boss){const kb=m.mesh.position.clone().sub(player.position).setY(0);if(kb.lengthSq()>.001)m.mesh.position.addScaledVector(kb.normalize(),.16)}updateTarget();floatDamage(dmg,m.mesh.position,crit);spawnHitFx(m.mesh.position,crit);if(!m.aggro)m.aggro=true;if(m.hp<=0)killMob(m);else if(tag!=='R')message(`${tag}: ${dmg}${crit?' KRİTİK':''} hasar.`)}
function floatDamage(n,pos,crit=false){const v=pos.clone();v.y+=2.7;v.project(camera);const d=document.createElement('div');d.className='damage';d.textContent=(crit?'CRIT ':'')+'-'+n;d.style.color=crit?'#fff199':'#ffdf91';d.style.fontSize=crit?'21px':'16px';d.style.left=((v.x*.5+.5)*innerWidth)+'px';d.style.top=((-v.y*.5+.5)*innerHeight)+'px';game.appendChild(d);setTimeout(()=>d.remove(),750)}
function queueAttack(m,{tag='R',damage=45,windup=180,recovery=440,power=1,aoe=false,range=2.55,canCrit=true}={}){const now=performance.now();if(pendingAttack||state.dead||now<state.hitStunUntil||now<state.dodgeUntil)return false;player.lookAt(m.mesh.position.x,player.position.y,m.mesh.position.z);pendingAttack={target:m,impactAt:now+windup,endAt:now+windup+recovery,tag,damage,aoe,range,canCrit,done:false};startAttackAnim(power,windup+recovery);return true}
function processPendingAttack(now){const a=pendingAttack;if(!a)return;if(!a.done&&now>=a.impactAt){a.done=true;spawnSlashFx(a.tag==='R'?1:1.12);if(a.aoe){for(const m of mobs)if(m.alive&&m.map===currentMap&&m.mesh.position.distanceTo(player.position)<3.35)hit(m,a.damage,a.tag,a.canCrit)}else if(a.target?.alive&&player.position.distanceTo(a.target.mesh.position)<=a.range)hit(a.target,a.damage,a.tag,a.canCrit)}if(now>=a.endAt)pendingAttack=null}
function basicAttack(){if(isSafeZone()){state.auto=false;renderHud();return}if(!target||!target.alive)return;const d=player.position.distanceTo(target.mesh.position);if(d>2.15){if(!pendingAttack)moveTarget=target.mesh.position.clone();return}moveTarget=null;const now=performance.now();if(pendingAttack||now-lastAttack<710||now<state.hitStunUntil||now<state.dodgeUntil)return;if(now<state.comboUntil)state.combo=Math.min(3,state.combo+1);else state.combo=1;state.comboUntil=now+1350;ui.combo.textContent=`COMBO x${state.combo}`;ui.combo.classList.add('on');lastAttack=now;const bonus=1+(state.combo-1)*.07;queueAttack(target,{tag:'R',damage:(30+Math.random()*18+state.level*3)*bonus,windup:state.combo===3?135:175,recovery:state.combo===3?350:425,power:1+.12*state.combo,range:2.45})}
function useSkill(slot){if(state.dead||pendingAttack||performance.now()<state.hitStunUntil||performance.now()<state.dodgeUntil)return;const id=bar[slot],sk=skills[id];if(!sk)return;if(isSafeZone()&&!sk.heal&&!sk.guard&&!sk.sprint){message('Safe zone içinde saldırı skilleri kullanılamaz.');return}const now=performance.now();if((cooldowns[id]||0)>now)return;if(state.mp<sk.mp){message('Yetersiz MP.');return}
if(sk.heal){state.hp=Math.min(state.maxHp,state.hp+sk.heal);state.mp-=sk.mp;cooldowns[id]=now+sk.cd}
else if(sk.guard){state.guardUntil=now+2500;state.mp-=sk.mp;cooldowns[id]=now+sk.cd;setCombatState('guard',now+600)}
else if(sk.sprint){state.sprintUntil=now+5500;state.stamina=Math.min(state.maxStamina,state.stamina+30);state.mp-=sk.mp;cooldowns[id]=now+sk.cd}
else if(sk.provoke){mobs.forEach(m=>{if(m.map===currentMap&&m.alive&&m.mesh.position.distanceTo(player.position)<10)m.aggro=true});state.mp-=sk.mp;cooldowns[id]=now+sk.cd}
else if(sk.dash){if(!target){message('Önce hedef seç.');return}const dir=target.mesh.position.clone().sub(player.position).setY(0);if(dir.length()>1.9){dir.setLength(Math.max(0,dir.length()-1.7));player.position.add(dir)}state.mp-=sk.mp;cooldowns[id]=now+sk.cd}
else{if(!target||!target.alive){message('Önce Z ile hedef seç.');return}if(player.position.distanceTo(target.mesh.position)>2.7){message('Hedef menzil dışında.');return}state.mp-=sk.mp;cooldowns[id]=now+sk.cd;const dmg=(38+Math.random()*16+state.level*4)*sk.pow;queueAttack(target,{tag:sk.n,damage:dmg,windup:sk.aoe?320:235,recovery:sk.aoe?620:480,power:sk.aoe?1.5:1.25,aoe:!!sk.aoe,range:2.8})}
renderHud();renderSlots()}
function renderHud(){ui.hp.style.width=(state.hp/state.maxHp*100)+'%';ui.hpText.textContent=`${Math.round(state.hp)} / ${state.maxHp}`;ui.mp.style.width=(state.mp/state.maxMp*100)+'%';ui.mpText.textContent=`${Math.round(state.mp)} / ${state.maxMp}`;ui.stam.style.width=(state.stamina/state.maxStamina*100)+'%';ui.stamText.textContent=`STA ${Math.round(state.stamina)} / ${state.maxStamina}`;ui.lvl.textContent=state.level;ui.np.textContent=state.np;ui.xp.style.width=(state.xp/state.nextXp*100)+'%';ui.coin.textContent=state.coins.toLocaleString('tr-TR');ui.inv.textContent=state.loot.length;ui.auto.innerHTML=`R: <b>${state.auto?'AÇIK':'